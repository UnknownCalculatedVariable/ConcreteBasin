local fterm = require("FTerm")

fterm.setup({
  border     = 'rounded', -- 'single', 'double', 'shadow', or 'rounded'
  dimensions = {
    height = 0.7,
    width  = 0.3,
    x      = 0.98,
    y      = 0.2,
  },
})

