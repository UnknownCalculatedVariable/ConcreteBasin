(defwidget wifi-indicator []
  (box :class "wifi"
    (button
      :class "wifi-button ${wifi-state}"
      :onclick "bar/scripts/wifi-toggle.sh"
      :tooltip "${wifi-ssid}"
      "${wifi-icon}")))
