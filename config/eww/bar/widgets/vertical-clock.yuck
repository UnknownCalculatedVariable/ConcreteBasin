(defwidget vertical-clock []
  (box :orientation "v" 
       :class "vertical-clock" 
       :valign "center"
    (label :tooltip "${date}" :class "vertical-clock-hour" :text hour)
    (label :tooltip "${date}" :class "vertical-clock-min" :text minute)))
