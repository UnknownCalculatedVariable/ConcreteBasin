(defwidget vertical-clock []
  (box :orientation "v" 
       :class "vertical-clock" 
       :valign "center"
    (label :tooltip "${date}" :class "clock-hour" :text hour)
    (label :tooltip "${date}" :class "clock-min" :text minute)))
